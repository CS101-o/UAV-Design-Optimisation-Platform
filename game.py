alpha = -infinity
Beta = +infinity


def maxNode(): 
    if the depth of the tree is not zero:

        get the head node

        for i in node.children:
                
            upate alpha if alpha < minNode.vlaue(i)
            if alpha => Beta: break the loop; prune

            return node.max()
    
            



def minNode(): 
if the depth of the tree is not zero:
    get the head node
    for i in node.children

    update beta if Beta > maxNode.value(i)
    if alpha => Beta: break the loop; prune

    return node.min()


def main(pass the tree): 
    if payer == white:
        maxNode(tree)




    



